CREATE TABLE TRANSFERS(
   ID              INTEGER PRIMARY KEY     NOT NULL,
   SOURCE_ID       INTEGER REFERENCES ACCOUNTS (ID)  NOT NULL,
   TARGET_ID       INTEGER REFERENCES ACCOUNTS (ID)   NOT NULL,
   AMOUNT        	 DECIMAL,
   TRANSFER_TIME   TIMESTAMP
);